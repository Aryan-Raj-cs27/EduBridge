import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "EduBridge AI Bot",
  initialMessages: [
    createChatBotMessage("Hello! How can I assist you today?"),
  ],
};

export default config;
